import React from 'react'
import { ProfilePassChange } from '../features/profile-pass-change/components/ProfilePassChange'

export const ProfilePassChangePage: React.FC = () => {
  return <ProfilePassChange />
}

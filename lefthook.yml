pre-commit:
  commands:
    frontend-lint:
      glob: '**/*.{ts,tsx}'
      run: cd packages/client && yarn lint
    frontend-pretty:
      glob: '**/*.{ts,tsx}'
      run: cd packages/client && yarn format
